/*interface inherits other ionterfaces using extends keyword
 * class use implements to inherit interfaces
 * */

package oopsConcepts;

public interface InterfaceB extends InterfaceA {
	
	void method10();
	void method11();

}


/*  Abstract class											Interface
 * it can have abstract and non- abstract methods			Can only have abstract methods
 * doesn't support mulitple inheritence							support multiple inheritence
 * they can have final, non-final, static and non-staitc		only have staic or final variables
 * abstract keyword is used 									interface keyword
 * uses extend keyword											implements keyword
 * */
